export class ThreeBackgroundTypeParticleData {
    constructor(directionChangeTime,color, speed, size ) {
    this.directionChangeTime = directionChangeTime;
    this.color = color;
    this.speed = speed;
    this.size = size;
    }
}